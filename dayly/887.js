/**
 * @param {number} k
 * @param {number} n
 * @return {number}
 */
var superEggDrop = function(k, n) {
    let count=0;
    while(count<n){
        let point=Math.round(n/2)
        let left=
        while(point)
    }
};